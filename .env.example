# Codex Environment Configuration
# Copy this file to .env and update values for your environment
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up

# =============================================================================
# REQUIRED - Must be set for production
# =============================================================================

# Secret key for session signing and encryption
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# =============================================================================
# OPTIONAL - Have sensible defaults
# =============================================================================

# Database configuration
# Default: SQLite in the data volume
DATABASE_URL=sqlite:///./data/codex_system.db

# Backend API port (external)
BACKEND_PORT=8765

# Frontend port (external)
FRONTEND_PORT=80

# Frontend development server port (only used with docker-compose.override.yml)
FRONTEND_DEV_PORT=5174

# API base URL for frontend build
# Use /api for production (nginx proxies to backend)
# Use http://localhost:8765 for development with frontend-dev
VITE_API_BASE_URL=/api

# Debug mode (set to true only for development)
DEBUG=false

# Logging configuration
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# JSON_LOGS: Use structured JSON logging (recommended for production)
JSON_LOGS=true

# =============================================================================
# OPTIONAL - Advanced Configuration
# =============================================================================

# Workspace path inside container
CODEX_WORKSPACE_PATH=/data/workspace

# CORS origins (comma-separated list for production)
# CORS_ORIGINS=https://your-domain.com
