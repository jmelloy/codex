"""Tests for markdown API endpoints - DISABLED.

Markdown API endpoints were removed as they were not used by the frontend.
These tests are kept for reference but disabled.
"""

import pytest

pytestmark = pytest.mark.skip(reason="Markdown endpoints removed - not used by frontend")


def test_markdown_removed():
    """Placeholder test to indicate markdown endpoints were removed."""
    pass
