"""Tests for plugin API endpoints - DISABLED.

Notebook plugin configuration endpoints and integration enable/disable endpoints
were removed as they were not used by the frontend.
These tests are kept for reference but disabled.
"""

import pytest

pytestmark = pytest.mark.skip(reason="Notebook plugin and integration enable endpoints removed - not used by frontend")


def test_plugin_endpoints_removed():
    """Placeholder test to indicate plugin configuration endpoints were removed."""
    pass
