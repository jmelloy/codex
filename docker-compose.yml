services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - 8765:8000
    environment:
      - DATABASE_URL=sqlite:////app/data/codex_system.db
      - SECRET_KEY=${SECRET_KEY:?SECRET_KEY is required}
      - DEBUG=${DEBUG:-false}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FORMAT=${LOG_FORMAT:-colored}
      - PYTHONUNBUFFERED=1
      - CODEX_PLUGINS_DIR=/app/plugins
      - OPENGRAPH_IO_API_KEY=${OPENGRAPH_IO_API_KEY}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI:-http://localhost:8065/oauth/callback}
      - PLUGIN_SERVICE_URL=${PLUGIN_SERVICE_URL:-http://plugin-service:8090}
      - PLUGIN_AUTO_SYNC=${PLUGIN_AUTO_SYNC:-true}
    volumes:
      - ./data:/app/data
      - ./plugins:/app/plugins
      - ./backend:/app
    depends_on:
      plugin-service:
        condition: service_healthy
    restart: unless-stopped
    command: >
      sh -c "alembic upgrade head && uvicorn codex.main:app --host 0.0.0.0 --port 8000 --reload"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  plugin-service:
    build:
      context: ./plugin-service
      dockerfile: Dockerfile
    ports:
      - 8090:8090
    environment:
      - PLUGIN_SERVICE_DIR=/app/plugins
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8090/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  frontend:
    image: node:22-slim
    working_dir: /app
    ports:
      - 8065:5173
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - VITE_PROXY_TARGET=http://backend:8000
    stdin_open: true
    tty: true
    depends_on:
      - backend

volumes:
  frontend-node-modules:
