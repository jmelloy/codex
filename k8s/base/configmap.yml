apiVersion: v1
kind: ConfigMap
metadata:
  name: codex-config
  namespace: codex
  labels:
    app.kubernetes.io/part-of: codex
data:
  DEBUG: "false"
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  CODEX_PLUGINS_DIR: "/app/plugins"
