apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: codex

resources:
  - namespace.yml
  - configmap.yml
  # secrets.yml removed — secrets are now per-environment via SOPS.
  # Each overlay includes a decrypted secrets file at deploy time.
  - pvc.yml
  - postgres-deployment.yml
  - backend-deployment.yml
  - frontend-deployment.yml
  - ingress.yml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
