# Template only - do NOT commit real secrets.
# In production, use: kubectl create secret generic codex-secrets \
#   --from-literal=SECRET_KEY=$(python3 -c "import secrets; print(secrets.token_hex(32))") \
#   -n codex
#
# Or use sealed-secrets / external-secrets-operator for GitOps.
apiVersion: v1
kind: Secret
metadata:
  name: codex-secrets
  namespace: codex
  labels:
    app.kubernetes.io/part-of: codex
type: Opaque
stringData:
  SECRET_KEY: "CHANGE-ME-generate-with-python-secrets-token-hex-32"
